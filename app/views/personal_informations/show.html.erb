<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN">
<html>
<head>
<title>営農情報</title>
<style type="text/css">
table, th, td {
  border-collapse: collapse;
  border: 1px solid #ccc;
  line-height: 1.5;
  font-size: large;
}
ul li {
  font-size: large;
}

table.table th {
  padding: 5px;
  font-weight: bold;
  vertical-align: top;
  background: #3f3f3f;
  color: #ffffff;
}
table.table td {
  padding: 5px;
  vertical-align: top;
}

label, span {
  font-size: x-large;
}

label {
  font-weight: bold;
}
</style>
</head>
<body>
<label>作業者:</label><span><%= @worker.name %></span>
<%= render(:partial => 'show_schedules') %>
<%= render(:partial => 'show_results') %>
<%= render(:partial => 'show_machines') %>
<%= render(:partial => 'show_lands') %>
<hr />
<%= mail_to @company.pc_mail, @company.home.name + " " + @company.first_name %>
</body>
</html>
