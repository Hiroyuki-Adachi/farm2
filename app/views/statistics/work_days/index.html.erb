<h1>世帯別作業日数一覧</h1>
<table class="table">
  <thead>
    <tr>
      <th>世帯</th>
      <% @systems.each do |system| %>
        <th class="numeric"><%= system.term_name %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @homes.each do |home| %>
      <tr>
        <th><%= home.name %></th>
        <% @systems.each do |system| %>
          <td class="numeric"><%= @work_days[[system.term, home.id]] %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to '戻る', menu_index_path, class: "btn btn-outline-dark" %>
