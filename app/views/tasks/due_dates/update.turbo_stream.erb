<%= turbo_stream.replace "task_due_on", partial: "tasks/due_dates/show" %>

<% frame_id = dom_id(@task, :due_on_modal_frame) %>
<% modal_id = dom_id(@task, :due_on_modal) %>
<%= turbo_stream.replace frame_id, partial: "tasks/close_modal", locals: { modal_id: modal_id } %>
