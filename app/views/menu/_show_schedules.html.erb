<h1 class="h4">予定</h1>
<ul class="list-group">
<% @schedules.each do |schedule| %>
  <li class="list-group-item"><%= schedule.worked_at %>から、<%= schedule.name %>の予定です。</li>
<% end %>
<% if @minute.present? %>
  <li class="list-group-item"><%= @minute.schedule.worked_at %>に<%= @minute.schedule.name %>がありました(<%= link_to '議事録', minute_path(@minute.id, token: params[:token]), {target: :_blank, data: {turbolinks: false}} %>)</li>
<% end %>
</ul>
