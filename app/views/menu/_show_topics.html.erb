<h1 class="h4" data-controller="page-loader" data-page-loader-modules-value="pages/menu-topics">ニュース</h1>
  <ul class="list-group">
  <% @user_topics.each do |user_topic| %>
    <li class="list-group-item">
      <span class='badge bg-primary me-1'><%= user_topic.word %></span>
      <span class='badge bg-secondary me-1'><%= user_topic.topic&.topic_type&.name %></span>
      <a href="<%= user_topic.topic.url %>" target="_blank" rel="noopener noreferrer" class="topic-link" data-readed="<%= users_word_path(user_topic.topic_id) %>">
        <%= user_topic.topic.title %>
      </a>
      <small class="text-muted">(<%= user_topic.topic.posted_on.strftime("%Y-%m-%d") %>)</small>
      <% unless user_topic.read_flag %>
        <span class="badge bg-warning text-dark">未読</span>
      <% end %>
    </li>
  <% end %>
</ul>
<div class="modal modal-xl fade" id="topic_data_modal" tabindex="-1" aria-labelledby="topicModalLabel" aria-hidden="true">
  <div class="modal-dialog" id="topic_data_dialog">
  </div>
</div>
