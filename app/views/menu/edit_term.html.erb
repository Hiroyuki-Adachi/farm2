<h1>対象年度変更</h1>
<%= error_print(@system) %>
<%= form_for(@system, url: {controller: :menu, action: :update }, html: {method: :put}) do |f| %>
  <%= f.select(:term, @terms) %>年度
  <br />
  <p>
  <%= f.submit '設定' %>
  <%= button_tag "戻る",  type: :button, :onclick => raw("javascript:location.href = '" + root_path + "';") %>
  </p>
<% end %>
